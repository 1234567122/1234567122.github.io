# 常州灵枢网络科技有限公司 - 网站项目文档

> **版本**: v2.0 (Next.js SSR + ThinkPHP API 架构版)
> **状态**: 待开发/重构中
> **核心目标**: 构建高性能、SEO友好、支持多语言及AI优化的企业官网。

---

## 1. 项目概述

- **公司名称**: 常州灵枢网络科技有限公司
- **品牌简称**: 灵枢科技
- **项目类型**: 企业官网 (前后端分离 + SSR服务端渲染)
- **核心业务**: GEO（生成式引擎优化）、AI驱动型数字营销服务
- **创始人**: 周鑫 (深耕互联网营销12年)
- **ICP备案**: 苏ICP备2026022216号

---

## 2. 整体架构设计 (标准化分层)

本项目采用标准化的四层架构设计，确保系统的稳定性与扩展性：

1. **前端界面层**: 负责用户体验、SEO抓取核心展示 (Next.js)。
2. **后端业务层**: 负责业务逻辑处理、数据中枢管理 (ThinkPHP)。
3. **服务器环境层**: 底层运行地基 (Linux + Nginx + Docker)。
4. **数据存储层**: 核心数据仓库 (MySQL + OSS)。
5.公司官网：https://www.ls56789.com
---

## 3. 技术栈详解

### 3.1 前端技术栈 (可视化交互)

| 模块 | 技术选型 | 核心作用 |
| :--- | :--- | :--- |
| **核心框架** | **Next.js** | 核心优势：**SSR服务端渲染**（对SEO极度友好），极速加载，原生多语言适配。 |
| **UI组件库** | React + Tailwind CSS | 组件化开发，提升效率；Tailwind实现高效开发与统一视觉规范。 |
| **基础三件套** | HTML5 / CSS3 / JS | 网页骨架、美化与动态交互核心。 |
| **流媒体技术** | M3U8 (HLS) | 视频自适应码率，全终端兼容，版权保护。 |
| **前端优化** | Service Worker | 缓存策略、离线访问、后台推送、数据同步。 |

### 3.2 后端技术栈 (业务中枢)

| 模块 | 技术选型 | 核心作用 |
| :--- | :--- | :--- |
| **主力语言** | **PHP 8.3** | 成熟稳定，安全升级，性能优异。 |
| **核心框架** | **ThinkPHP** | 适合询盘管理、产品后台及API对接，开发效率高。 |
| **通信标准** | JSON + RESTful API | 前后端分离的标准数据交换格式。 |
| **架构模式** | 前后端分离 | 前端负责页面渲染与交互；后端专注业务逻辑与数据安全。 |

### 3.3 服务器与运行环境 (底层地基)

- **操作系统**: Linux (高稳定+高安全+高性能)
- **Web容器**:
    - **Nginx**: 全局入口，流量转发，安全防护，SEO配置。
    - **Node.js**: 运行 Next.js 服务，处理 SSR 渲染，对接后端 API。
- **运维工具**: Docker (环境统一 + 简化部署)
- **对象存储**: OSS (海量存储 + 高可靠 + 联动 CDN)
- **全国加速**: CDN (就近访问 + 降延迟 + 隐藏源站 + 防攻击)
- **基础设施**: 腾讯云2核2G的服务器

### 3.4 数据库 (核心数据仓库)

- **选型**: MySQL
- **存储内容**: 产品信息、询盘记录、客户资料、网站配置。
- **优势**: ThinkPHP 原生支持，操作高效，安全稳定。

---

## 4. 数据交换与 AI SEO 增强

为了配合公司核心的 **GEO (生成式引擎优化)** 业务，本站将作为最佳实践案例：

1. **Schema结构化数据**: 全面部署搜索引擎/AI的“网页说明书”，提升收录与点击率。
2. **JSON-LD代码**: Schema标准落地，谷歌优先识别，不影响页面性能。
3. **llms.txt**: 专门为大模型爬虫准备的知识库文件，主动投喂信息，抢占AI搜索流量。
4. **SSR服务端渲染**: 保障 AI 爬虫能完整抓取页面内容（而非空白JS）。

---

## 5. 核心业务场景串联

### 用户提交询盘流程
1. **前端**: Next.js 收集表单数据 -> 调用 API。
2. **后端**: ThinkPHP 校验数据 -> 存入 MySQL -> 返回成功状态。
3. **展示**: 后台管理系统实时显示。

### 访问产品详情页流程
1. **请求**: Nginx 转发请求 -> Node.js 运行 Next.js。
2. **渲染**: 服务端获取数据 -> SSR 渲染 -> 返回带结构化数据的 HTML。

### 切换网站语言流程
1. **机制**: 加载对应文案 -> 输出 `hreflang` 标签 -> 规避多语言 SEO 问题。

---

## 6. 开发规范与文件结构

### 目录结构预览

```text
project-root/
├── frontend/              # Next.js 前端项目
│   ├── app/               # App Router 页面路由
│   ├── components/        # 公共组件 (Header, Footer)
│   ├── public/            # 静态资源 (images, fonts)
│   └── ...
├── backend/               # ThinkPHP 后端项目
│   ├── app/               # 应用目录 (Controller, Model)
│   ├── config/            # 配置文件
│   └── ...
├── docker-compose.yml     # Docker 编排文件
└── README.md